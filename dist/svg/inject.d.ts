/**
 * Creates the hidden SVG element, populates it with the filter definitions,
 * and appends it to <body>.  Idempotent — only runs once per init cycle.
 */
export declare function _injectSVG(): void;
